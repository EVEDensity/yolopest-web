# 忽略环境文件
/backend-old

# IDE 配置（每个人的IDE不同，不要提交）
.vscode/
.idea/

# Claude Code 配置（包含本地权限路径，不要提交）
.claude/

# 环境变量密钥文件（所有层级，绝不允许提交）
.env
.env.*
!.env.example
!backend/.env.template

# 旧版后端副本
backend/pest_Uniapp_backend/

# 无意义文件
backend/.jpg